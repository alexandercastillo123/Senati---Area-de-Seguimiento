// Se colocaran todas las interfaces relacionadas al usuario, como el login, etc
export interface Usuario {
    email: string;
    password: string;
}


export interface UsuarioLogin {
    email: string;
    password: string;
}